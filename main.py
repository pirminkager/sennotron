import pid
#import display.py as *
#import ad



if __name__ == "__main__":
  while True:
    if state == 0:

    elif state == 1:
      #step

      if nextstep:
	state = state + 1
	nextstep = 0 
    elif state == 2:

    elif state == 3:

    elif state == 4:

    else:
      state = 0

    if skip
